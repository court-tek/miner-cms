<section class="container">
  <div class="row">
    <div class="col-sm-12">
      <div class="modal">
        <p style="color: green"><%= notice %></p>

        <%= render @page %>

        <div>
            <%= link_to "Edit this item", edit_page_path(@page) %> |
            <%= link_to "Back to menu", food_path %>

            <%= button_to "Destroy this item", @page, method: :delete %>
        </div>
      </div>
    </div>
  </div>
</section>
